# Copy this file to `.env` (it is gitignored) and edit as needed.
#
# Then start the gateway with:
#   ./scripts/serve.sh

# Minimal config: use a preset to avoid tuning lots of env vars.
# CODEX_PRESET=codex-fast

# Codex defaults. Keep CODEX_AUTO_UPDATE_CLI enabled so startup refreshes the
# local Codex CLI when newer models require a newer client version.
# CODEX_MODEL=gpt-5.6-sol
# Optional override for GET /v1/models. If unset, the gateway fetches live models
# from the connected provider (Codex catalog, cursor-agent --list-models, Claude
# /v1/models, Gemini model list) and falls back to a built-in catalog.
# CODEX_ADVERTISED_MODELS=gpt-5.6-sol,gpt-5.6-terra,gpt-5.6-luna,gpt-5.5
# CODEX_ALLOW_CLIENT_MODEL_OVERRIDE=1
# CODEX_AUTO_UPDATE_CLI=1

# Optional auth (if set, clients must send: Authorization: Bearer <token>)
CODEX_GATEWAY_TOKEN=devtoken

# Optional overrides (usually don't need these if using a preset)
# CODEX_WORKSPACE=/path/to/workspace
# If unset, the gateway uses an empty temp dir under /tmp by default.
# CODEX_LOG_MODE=qa
CODEX_LOG_EVENTS=1
# CODEX_LOG_REQUEST_CURL=1
# CODEX_CLI_HOME=/absolute/path/to/.codex-gateway-home
CODEX_CODEX_ALLOW_TOOLS=1
# Prompt size cap. Long resumed chats are trimmed to this budget instead of 413.
# CODEX_MAX_PROMPT_CHARS=200000
#
# For advanced tuning and multi-provider env vars, see README.md.
